<UserControl x:Class="ModernApplicationFramework.Extended.Demo.Modules.UIContextChange.TriggerUiContextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Button Height="21" VerticalAlignment="Top" Content="Activate Context 1" Command="{Binding ActivateContextCommand}"/>
        <Button Height="21" VerticalAlignment="Top" Content="Deactivate Context 1" Command="{Binding DeactivateContextCommand}"/>
        <Button Height="21" VerticalAlignment="Top" Content="Activate Context 2" Command="{Binding ActivateContext2Command}"/>
        <Button Height="21" VerticalAlignment="Top" Content="Deactivate Context 2" Command="{Binding DeactivateContext2Command}"/>
    </StackPanel>
</UserControl>
