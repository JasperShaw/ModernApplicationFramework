<UserControl x:Class="ModernApplicationFramework.Extended.Demo.Modules.InfoBarDemo.InfoBarDemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button Content="Show Info Bar (Main Host)" Margin="20,0,20,0" Command="{Binding ShowInfoBarCommand}"/>
            <Button Content="Open Demo Tool Window" Margin="20,0,20,0" Command="{Binding OpenDemoToolCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
