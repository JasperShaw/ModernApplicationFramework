<UserControl x:Class="ModernApplicationFramework.MVVM.Demo.Modules.UndoRedoTest.UndoRedoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="69*"/>
            <RowDefinition Height="181*"/>
        </Grid.RowDefinitions>

        <TextBox Text="{Binding Text, Mode=TwoWay}"/>
        <Button Margin="0,50,0,0" Grid.Row="1" Command="{Binding SetValueCommand}">SetValue</Button>
        <Button Margin="0,50,0,0" Grid.Row="2" Command="{Binding UndoCommand}">Undo</Button>

    </Grid>
</UserControl>
