<UserControl x:Class="ModernApplicationFramework.Extended.Settings.Views.MainWindowSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mafc="http://modern.framework.com/controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="460">
    
    <StackPanel>
        <mafc:GroupBox Header="Test" CornerRadius="0">
            <StackPanel Orientation="Horizontal">
                <Label MinWidth="100">Color Theme:</Label>
                <ComboBox ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" MinWidth="100">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </mafc:GroupBox>
    </StackPanel>
</UserControl>
